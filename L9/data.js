"use strict";
// const userInput = document.getElementById("input-field"); // here when we hover at userInput then it saw us that it return either HTMLelement or the null to solve that error null we add ! last
// userInput.value = "Hi"
// upper problem we get to solve that we use below insertion ! and <HTMLInputElement>
const userInput = document.getElementById("input-field"); // now when we hover then only we get the null & <HTMlelement>
userInput.value = "Hi";
//now automatically it apply into the input field if you go to the browser for the UI
